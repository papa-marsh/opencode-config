---
description: Meta evaluation of an entire session, intended to identify opportunities to improve and extend the landscape of context artifacts across the user's opencode config directory and any relevant repos.
agent: go
---

## Session Context Capture

We're going to pivot into a meta retrospective of this agent session in its entirety. Our task is to identify any opportunities to improve the landscape of context artifacts across the user's opencode config (`~/.config/opencode`) and any relevant repos. 

Our goal is to facilitate the natural evolution of context. Some sessions will not yield anything that adds value. That's fine; **additions and modifications must earn their place**.

Let's work together to determine if there is anything worth capturing from this session; and if so, how best to cleanly integrate it into new or existing artifacts.

To start, load `~/.config/opencode/AGENTS.md` and the `platform` and `authoring-context` skills.

It is critical that you are aware of what context artifacts already exist. Explore the user's opencode config directory to orient yourself around what already exists. If anything seems relevant to what was done during this session, read the contents so that you can effectively evaluate any gaps or inconsistencies.

---

### Context Artifact Examples

Context is persisted across different layers of the agentic architecture and in repos themselves. Artifacts to consider adding/extending include but are not limited to:

- Gobal opencode `AGENTS.md` file
- Opencode primitives (skills, commands, agents)
- Agent context artifacts in `~/.config/opencode/context/`
- Repo `README.md`, `CLAUDE.md`, and `AGENTS.md` files
- Documentation that lives within a repo

### Dimensions to Consider

The following are illustrative, not prescriptive. They are intended to guide your thinking, not serve as a checklist. You, the agent, should sensibly determine what is relevant and important.

- Did we learn about a new repo or interaction that should be added to the `platform` skill? 
- Did we deepen our understanding of any domain, component, or service in a way that should be persisted in a skill or `~/.config/opencode/context/` file?
- Did we run into friction, inconsistencies, or incompleteness when loading a skill or reading documentation?
- Are all the docs/context/skill artifacts complete and up-to-date given our current understanding?
- Was a new feature added or discovered that should be added to a repo's `AGENTS.md` file?


---

You must understand the current state of the context landscape to perform this task effectively. You may also take the time to research anything else that you deem helpful in facilitating the evolution of context.

All context sources play a part in the broader contextual landscape; think through this task holistically in a way that will improve future agent sessions.

Before proceeding, briefly summarize your understanding of the goal of this retro exercise, then proceed with any relevant research.

Once satisfied with the evaluation, present recommendations as a starting point for collaboration with the user.
